<h2>v0.0.0-beta.8 - Datagen cleanup</h2>

<p>Internal fixes, no visible gameplay changes.</p>

<h3>Fixed</h3>
<ul>
<li>Duplicate data files that made the project's data generator fail to run.</li>
<li>The altar and pillar blocks now use their own 3D model as their inventory icon instead of a flat placeholder.</li>
</ul>

<blockquote><strong>Early alpha.</strong> Custom item/block textures are still in progress &#8212; expect the classic missing-texture checkerboard on some items until then.</blockquote>
